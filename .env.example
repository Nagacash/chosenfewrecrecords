# Too Lost API (server-only — never expose to the browser)
TOOLOST_CLIENT_ID=
TOOLOST_CLIENT_SECRET=
TOOLOST_REFRESH_TOKEN=

# Optional: YouTube Data API v3 (falls back to curated list when unset)
YOUTUBE_API_KEY=
